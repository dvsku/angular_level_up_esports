export class ProductInfoSize {
    id?: number;
    productSize?: string;
    sizeOrder?: number;

    constructor(id?: number, productSize?: string) {
        this.id = id;
        this.productSize = productSize;
    }
}
